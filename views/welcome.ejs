<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Welcome Transition</title>
  <style>
    html, body {
      margin: 0;
      height: 100%;
      overflow: hidden;
      display: flex;
      justify-content: center;
      align-items: center;
      background-color: black;
    }

    .zoom-text {
      font-size: 15vw;
      font-weight: bold;
      background: url('/background.jpg') center center no-repeat;
      background-size: cover;
      color: transparent;
      -webkit-background-clip: text;
      background-clip: text;
      animation: zoomThrough 2.5s ease-in-out forwards;
    }

    @keyframes zoomThrough {
      0% {
        transform: scale(1);
        opacity: 1;
      }
      100% {
        transform: scale(30);
        opacity: 0;
      }
    }
  </style>
</head>
<body>
  <div class="zoom-text">WELCOME</div>

  <script>
    setTimeout(() => {
      window.location.href = "/index";
    }, 2500);
  </script>
</body>
</html>
